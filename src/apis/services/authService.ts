const services: any = {};

services.getUser = async (username?: string, email?: string) => {
  return true;
};

services.registerUser = async (user: any) => {};

services.sendEmailOtp = async (email: string) => {};

export default services;
